import 'package:flutter/material.dart';
import 'package:flutter_components/components/widget_button.dart';

widgetRowColumn() {
  return Row(
    children: [widgetButton(), widgetButton(), widgetButton()],
  );
}
